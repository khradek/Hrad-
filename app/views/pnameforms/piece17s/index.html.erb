<h1></h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @piece17s.each do |piece17| %>
      <tr>
        <td><%= piece17.name %></td>
        <td><%= link_to 'Edit', edit_pnameform_piece17_path(@pnameform, piece17) %></td>
        <td><%= link_to 'Destroy', [@pnameform, piece17], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Piece17', new_pnameform_piece17_path %>
<%= link_to 'Back', @pnameform %>
