<h1><%= @pnameform.pname17 %>s</h1>

<div class="media">
  <div class="media-body">
    <table class="table table hover">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @piece17s.each do |piece17| %>
        <tr>
          <td><strong><%= piece17.name %></strong></td>
          <td><%= link_to 'Edit', edit_pnameform_piece17_path(@pnameform, piece17) %></td>
          <td><%= link_to 'Delete', [@pnameform, piece17], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
    </table>
  </div>  
</div> 
<br>
<%= link_to "New #{@pnameform.pname17}", new_pnameform_piece17_path %>
<%= link_to 'Back', @pnameform %>
