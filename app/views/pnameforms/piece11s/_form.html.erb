<%= form_for [@pnameform, @piece11] do |f| %>
  <% if @piece11.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece11.errors.count, "error") %> prohibited this piece11 from being saved:</h2>

      <ul>
      <% @piece11.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
