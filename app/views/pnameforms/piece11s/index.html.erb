<h1></h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @piece11s.each do |piece11| %>
      <tr>
        <td><%= piece11.name %></td>
        <td><%= link_to 'Edit', edit_pnameform_piece11_path(@pnameform, piece11) %></td>
        <td><%= link_to 'Destroy', [@pnameform, piece11], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Piece11', new_pnameform_piece11_path %>
<%= link_to 'Back', @pnameform %>
