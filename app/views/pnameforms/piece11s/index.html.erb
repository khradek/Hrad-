<h1><%= @pnameform.pname11 %>s</h1>

<div class="media">
  <div class="media-body">
    <table class="table table hover">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @piece11s.each do |piece11| %>
        <tr>
          <td><strong><%= piece11.name %></strong></td>
          <td><%= link_to 'Edit', edit_pnameform_piece11_path(@pnameform, piece11) %></td>
          <td><%= link_to 'Delete', [@pnameform, piece11], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
    </table>
  </div>  
</div> 
<br>

<%= link_to "New #{@pnameform.pname11}", new_pnameform_piece11_path %>
<%= link_to 'Back', @pnameform %>
