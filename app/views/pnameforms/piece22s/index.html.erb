<h1></h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @piece22s.each do |piece22| %>
      <tr>
        <td><%= piece22.name %></td>
        <td><%= link_to 'Edit', edit_pnameform_piece22_path(@pnameform, piece22) %></td>
        <td><%= link_to 'Destroy', [@pnameform, piece22], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Piece22', new_pnameform_piece22_path %>
<%= link_to 'Back', @pnameform %>
