<h1><%= @pnameform.pname22 %>s</h1>

<div class="media">
  <div class="media-body">
    <table class="table table hover">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @piece22s.each do |piece22| %>
        <tr>
          <td><strong><%= piece22.name %></strong></td>
          <td><%= link_to 'Edit', edit_pnameform_piece22_path(@pnameform, piece22) %></td>
          <td><%= link_to 'Delete', [@pnameform, piece22], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
    </table>
  </div>  
</div> 
<br>

<%= link_to "New #{@pnameform.pname22}", new_pnameform_piece22_path %>
<%= link_to 'Back', @pnameform %>
