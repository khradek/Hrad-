<h1><%= @pnameform.pname16 %>s</h1>

<div class="media">
  <div class="media-body">
    <table class="table table hover">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @piece16s.each do |piece16| %>
        <tr>
          <td><strong><%= piece16.name %></strong></td>
          <td><%= link_to 'Edit', edit_pnameform_piece16_path(@pnameform, piece16) %></td>
          <td><%= link_to 'Delete', [@pnameform, piece16], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
    </table>
  </div>  
</div> 
<br>

<%= link_to "New #{@pnameform.pname16}", new_pnameform_piece16_path %>
<%= link_to 'Back', @pnameform %>
