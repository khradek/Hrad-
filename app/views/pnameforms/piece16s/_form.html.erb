<%= form_for [@pnameform, @piece16] do |f| %>
  <% if @piece16.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece16.errors.count, "error") %> prohibited this piece16 from being saved:</h2>

      <ul>
      <% @piece16.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
