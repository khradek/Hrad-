<h1><%= @pnameform.pname19 %>s</h1>

<div class="media">
  <div class="media-body">
    <table class="table table hover">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @piece19s.each do |piece19| %>
        <tr>
          <td><strong><%= piece19.name %></strong></td>
          <td><%= link_to 'Edit', edit_pnameform_piece19_path(@pnameform, piece19) %></td>
          <td><%= link_to 'Delete', [@pnameform, piece19], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
    </table>
  </div>  
</div> 
<br>

<%= link_to "New #{@pnameform.pname19}", new_pnameform_piece19_path %>
<%= link_to 'Back', @pnameform %>
