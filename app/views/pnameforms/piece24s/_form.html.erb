<%= form_for [@pnameform, @piece24] do |f| %>
  <% if @piece24.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece24.errors.count, "error") %> prohibited this piece24 from being saved:</h2>

      <ul>
      <% @piece24.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit "Create" %>
  </div>
<% end %>
