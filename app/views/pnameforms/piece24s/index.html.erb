<h1></h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @piece24s.each do |piece24| %>
      <tr>
        <td><%= piece24.name %></td>
        <td><%= link_to 'Edit', edit_pnameform_piece24_path(@pnameform, piece24) %></td>
        <td><%= link_to 'Destroy', [@pnameform, piece24], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Piece24', new_pnameform_piece24_path %>
<%= link_to 'Back', @pnameform %>
