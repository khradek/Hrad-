<%= form_for [@pnameform, @piece1] do |f| %>
  <% if @piece1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece1.errors.count, "error") %> prohibited this piece1 from being saved:</h2>

      <ul>
      <% @piece1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
