<%= form_for [@pnameform, @piece15] do |f| %>
  <% if @piece15.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece15.errors.count, "error") %> prohibited this piece15 from being saved:</h2>

      <ul>
      <% @piece15.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
