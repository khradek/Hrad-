<h1><%= @pnameform.pname15 %>s</h1>

<div class="media">
  <div class="media-body">
    <table class="table table hover">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @piece15s.each do |piece15| %>
        <tr>
          <td><strong><%= piece15.name %></strong></td>
          <td><%= link_to 'Edit', edit_pnameform_piece15_path(@pnameform, piece15) %></td>
          <td><%= link_to 'Delete', [@pnameform, piece15], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
    </table>
  </div>  
</div> 
<br>

<%= link_to "New #{@pnameform.pname15}", new_pnameform_piece15_path %>
<%= link_to 'Back', @pnameform %>
