<%= form_for [@pnameform, @piece5] do |f| %>
  <% if @piece5.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece5.errors.count, "error") %> prohibited this piece5 from being saved:</h2>

      <ul>
      <% @piece5.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
