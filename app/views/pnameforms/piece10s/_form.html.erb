<%= form_for [@pnameform, @piece10] do |f| %>
  <% if @piece10.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece10.errors.count, "error") %> prohibited this piece10 from being saved:</h2>

      <ul>
      <% @piece10.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit "Create" %>
  </div>
<% end %>
