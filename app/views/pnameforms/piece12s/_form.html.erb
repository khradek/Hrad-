<%= form_for [@pnameform, @piece12] do |f| %>
  <% if @piece12.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece12.errors.count, "error") %> prohibited this piece12 from being saved:</h2>

      <ul>
      <% @piece12.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
