<h1></h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @piece12s.each do |piece12| %>
      <tr>
        <td><%= piece12.name %></td>
        <td><%= link_to 'Edit', edit_pnameform_piece12_path(@pnameform, piece12) %></td>
        <td><%= link_to 'Destroy', [@pnameform, piece12], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Piece12', new_pnameform_piece12_path %>
<%= link_to 'Back', @pnameform %>
