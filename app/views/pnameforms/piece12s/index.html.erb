<h1><%= @pnameform.pname12 %>s</h1>

<div class="media">
  <div class="media-body">
    <table class="table table hover">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @piece12s.each do |piece12| %>
        <tr>
          <td><strong><%= piece12.name %></strong></td>
          <td><%= link_to 'Edit', edit_pnameform_piece12_path(@pnameform, piece12) %></td>
          <td><%= link_to 'Delete', [@pnameform, piece12], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
    </table>
  </div>  
</div> 
<br>

<%= link_to "New #{@pnameform.pname12}", new_pnameform_piece12_path %>
<%= link_to 'Back', @pnameform %>
