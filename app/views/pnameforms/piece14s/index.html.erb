<h1><%= @pnameform.pname14 %>s</h1>

<div class="media">
  <div class="media-body">
    <table class="table table hover">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @piece14s.each do |piece14| %>
        <tr>
          <td><strong><%= piece14.name %></strong></td>
          <td><%= link_to 'Edit', edit_pnameform_piece14_path(@pnameform, piece14) %></td>
          <td><%= link_to 'Delete', [@pnameform, piece14], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
    </table>
  </div>  
</div> 
<br>

<%= link_to "New #{@pnameform.pname14}", new_pnameform_piece14_path %>
<%= link_to 'Back', @pnameform %>
