<%= form_for [@pnameform, @piece23] do |f| %>
  <% if @piece23.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece23.errors.count, "error") %> prohibited this piece23 from being saved:</h2>

      <ul>
      <% @piece23.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>