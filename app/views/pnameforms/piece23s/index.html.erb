<h1><%= @pnameform.pname23 %>s</h1>

<div class="media">
  <div class="media-body">
    <table class="table table hover">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @piece23s.each do |piece23| %>
        <tr>
          <td><strong><%= piece23.name %></strong></td>
          <td><%= link_to 'Edit', edit_pnameform_piece23_path(@pnameform, piece23) %></td>
          <td><%= link_to 'Delete', [@pnameform, piece23], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
    </table>
  </div>  
</div> 
<br>

<%= link_to "New #{@pnameform.pname23}", new_pnameform_piece23_path %>
<%= link_to 'Back', @pnameform %>
