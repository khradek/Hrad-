<%= form_for [@pnameform, @piece7] do |f| %>
  <% if @piece7.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece7.errors.count, "error") %> prohibited this piece7 from being saved:</h2>

      <ul>
      <% @piece7.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
