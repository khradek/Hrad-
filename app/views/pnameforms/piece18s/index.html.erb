<h1></h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @piece18s.each do |piece18| %>
      <tr>
        <td><%= piece18.name %></td>
        <td><%= link_to 'Edit', edit_pnameform_piece18_path(@pnameform, piece18) %></td>
        <td><%= link_to 'Destroy', [@pnameform, piece18], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Piece18', new_pnameform_piece18_path %>
<%= link_to 'Back', @pnameform %>
