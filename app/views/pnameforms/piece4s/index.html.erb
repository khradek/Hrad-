<h1><%= @pnameform.pname4 %>s</h1>

<div class="media">
  <div class="media-body">
    <table class="table table hover">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @piece4s.each do |piece4| %>
        <tr>
          <td><strong><%= piece4.name %></strong></td>
          <td><%= link_to 'Edit', edit_pnameform_piece4_path(@pnameform, piece4) %></td>
          <td><%= link_to 'Delete', [@pnameform, piece4], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
    </table>
  </div>  
</div> 
<br>

<%= link_to "New #{@pnameform.pname4}", new_pnameform_piece4_path %>
<%= link_to 'Back', @pnameform %>
