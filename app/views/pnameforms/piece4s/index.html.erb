<h1></h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @piece4s.each do |piece4| %>
      <tr>
        <td><%= piece4.name %></td>
        <td><%= link_to 'Edit', edit_pnameform_piece4_path(@pnameform, piece4) %></td>
        <td><%= link_to 'Destroy', [@pnameform, piece4], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Piece4', new_pnameform_piece4_path %>
<%= link_to 'Back', @pnameform %>
