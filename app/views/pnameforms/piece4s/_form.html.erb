<%= form_for [@pnameform, @piece4] do |f| %>
  <% if @piece4.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece4.errors.count, "error") %> prohibited this piece4 from being saved:</h2>

      <ul>
      <% @piece4.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
