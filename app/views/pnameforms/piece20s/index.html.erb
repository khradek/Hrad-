<h1></h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @piece20s.each do |piece20| %>
      <tr>
        <td><%= piece20.name %></td>
        <td><%= link_to 'Edit', edit_pnameform_piece20_path(@pnameform, piece20) %></td>
        <td><%= link_to 'Destroy', [@pnameform, piece20], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Piece20', new_pnameform_piece20_path %>
<%= link_to 'Back', @pnameform %>
