<h1><%= @pnameform.pname20 %>s</h1>

<div class="media">
  <div class="media-body">
    <table class="table table hover">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @piece20s.each do |piece20| %>
        <tr>
          <td><strong><%= piece20.name %></strong></td>
          <td><%= link_to 'Edit', edit_pnameform_piece20_path(@pnameform, piece20) %></td>
          <td><%= link_to 'Delete', [@pnameform, piece20], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
    </table>
  </div>  
</div> 
<br>

<%= link_to "New #{@pnameform.pname20}", new_pnameform_piece20_path %>
<%= link_to 'Back', @pnameform %>
