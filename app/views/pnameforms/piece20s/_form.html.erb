<%= form_for [@pnameform, @piece20] do |f| %>
  <% if @piece20.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece20.errors.count, "error") %> prohibited this piece20 from being saved:</h2>

      <ul>
      <% @piece20.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
