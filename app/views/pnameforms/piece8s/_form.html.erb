<%= form_for [@pnameform, @piece8] do |f| %>
  <% if @piece8.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece8.errors.count, "error") %> prohibited this piece8 from being saved:</h2>

      <ul>
      <% @piece8.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
