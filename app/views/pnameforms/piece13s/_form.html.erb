<%= form_for [@pnameform, @piece13] do |f| %>
  <% if @piece13.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece13.errors.count, "error") %> prohibited this piece13 from being saved:</h2>

      <ul>
      <% @piece13.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
