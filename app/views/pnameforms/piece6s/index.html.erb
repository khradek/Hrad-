<h1><%= @pnameform.pname6 %>s</h1>

<div class="media">
  <div class="media-body">
    <table class="table table hover">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @piece6s.each do |piece6| %>
        <tr>
          <td><strong><%= piece6.name %></strong></td>
          <td><%= link_to 'Edit', edit_pnameform_piece6_path(@pnameform, piece6) %></td>
          <td><%= link_to 'Delete', [@pnameform, piece6], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
    </table>
  </div>  
</div> 
<br>
<%= link_to "New #{@pnameform.pname6}", new_pnameform_piece6_path %>
<%= link_to 'Back', @pnameform %>
