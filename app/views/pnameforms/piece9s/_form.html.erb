<%= form_for [@pnameform, @piece9] do |f| %>
  <% if @piece9.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece9.errors.count, "error") %> prohibited this piece9 from being saved:</h2>

      <ul>
      <% @piece9.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit "Create" %>
  </div>
<% end %>
