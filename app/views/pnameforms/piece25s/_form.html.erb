<%= form_for [@pnameform, @piece25] do |f| %>
  <% if @piece25.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece25.errors.count, "error") %> prohibited this piece25 from being saved:</h2>

      <ul>
      <% @piece25.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
