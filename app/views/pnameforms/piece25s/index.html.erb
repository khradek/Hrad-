<h1><%= @pnameform.pname25 %>s</h1>

<div class="media">
  <div class="media-body">
    <table class="table table hover">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @piece25s.each do |piece25| %>
        <tr>
          <td><strong><%= piece25.name %></strong></td>
          <td><%= link_to 'Edit', edit_pnameform_piece25_path(@pnameform, piece25) %></td>
          <td><%= link_to 'Delete', [@pnameform, piece25], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
    </table>
  </div>  
</div> 
<br>

<%= link_to "New #{@pnameform.pname25}", new_pnameform_piece25_path %>
<%= link_to 'Back', @pnameform %>
