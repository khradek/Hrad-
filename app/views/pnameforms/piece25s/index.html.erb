<h1></h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @piece25s.each do |piece25| %>
      <tr>
        <td><%= piece25.name %></td>
        <td><%= link_to 'Edit', edit_pnameform_piece25_path(@pnameform, piece25) %></td>
        <td><%= link_to 'Destroy', [@pnameform, piece25], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Piece25', new_pnameform_piece25_path %>
<%= link_to 'Back', @pnameform %>
